{
  "name": "0g-hk",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "wrangler dev",
    "deploy": "wrangler deploy",
    "tail": "wrangler tail",
    "typecheck": "tsc --noEmit",
    "types": "wrangler types",
    "test": "vitest",
    "test:run": "vitest run",
    "test:preserve:record": "PRESERVE=record vitest run"
  },
  "devDependencies": {
    "@cloudflare/vitest-pool-workers": "^0.16.3",
    "fast-check": "^4.9.0",
    "typescript": "^5.6.3",
    "vitest": "^4.1.5",
    "wrangler": "^4.82.0"
  },
  "dependencies": {
    "hono": "^4.12.18"
  }
}
